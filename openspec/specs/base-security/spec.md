## 新增需求

### 需求: 物理路径去模糊与防逃逸
智能体在处理所有高层文件 API 操作（读取、写入、修改、列目录）以及终端工作目录（cwd）时，必须（MUST）对目标路径调用 `fs.realpathSync` 物理绝对路径解析，并强制（SHALL）要求其位于标准化后的工作区物理绝对路径边界内。

#### 场景: 工作区内正常读写
- **WHEN** 智能体试图读取工作区内的相对路径文件 `./package.json`
- **THEN** 路径校验器校验成功，将路径解析为对应的真实物理绝对路径，并允许文件操作执行。

#### 场景: 软链接沙箱逃逸拦截
- **WHEN** 工作区内存在一个符号链接 `./link_to_system` 指向外部 `C:\Windows`，智能体试图读取 `./link_to_system/system.ini`
- **THEN** 路径校验器通过 `fs.realpathSync` 展开发现真实物理路径为 `C:\Windows\system.ini`，判定其溢出安全边界，抛出拒绝访问的错误强行拦截。

### 需求: 终端写倾向命令拦截与安全降级
在终端命令 `execute_command` 拦截过程中，系统必须（MUST）通过敏感命令及常见别名正则审查输入命令。除了明确安全的只读白名单指令（如 `git status`、`git diff`）外，凡是包含写倾向、管道混写或未识别的复杂脚本，系统应当（SHALL）将其安全级别降级为 `behavior: 'ask'` 并挂起执行，拉起交互弹窗提问，不得静默放行。在 `Plan` 模式下，系统必须（MUST）将可进入审批的终端命令集合限定为**可静态证明安全的系统只读查询**：即该命令必须同时满足（1）命中只读白名单前缀、且（2）不包含任何复合连接符（`|`、`&`、`;`）、重定向符（`>`、`<`）、环境变量展开符（`%`）或命令替换语法（`$(`、`` ` ``）。满足上述条件的命令在 Plan 模式下应当（SHALL）进入统一审批流程；不满足的命令必须（MUST）予以硬拦截并返回 `BLOCKED (Plan Mode Only)` 错误，且错误消息中必须（MUST）附带针对大模型的引导性自愈提示，告知其应改用何种只读原生工具（如 `list_dir` / `read_file` 等）或指示其将高危改动在计划中进行合理表述。前置安全判定与执行期结构校验必须在允许集合上严格同构——凡是被前置判定拒绝的命令，执行期也必须拒绝；凡是进入审批并被放行的命令，执行期也必须通过。

#### 场景: 只读命令静默放行
- **WHEN** 智能体试图在终端执行只读白名单内的命令 `git status`
- **THEN** 拦截器识别其为无风险只读指令，直接静默放行，无需用户审批。

#### 场景: 危险写倾向命令降级审批
- **WHEN** 智能体试图在终端执行具有写倾向的删除命令 `Remove-Item -Force d:\Projects\MyAgent\dist`
- **THEN** 拦截器触发敏感命令正则与非只读判定，自动挂起命令执行，将命令的完整内容通过 `Ask` 机制展示，等待用户裁决。

#### 场景: Plan 模式下可静态证明安全的只读命令进入审批
- **WHEN** 智能体在 `Plan` 模式下试图在终端执行只读白名单内且无复合符号的系统查询命令 `dir /-C /w C:\Windows\Temp`
- **THEN** 拦截器判定该命令同时满足白名单匹配和无复合字符两个条件，识别为可静态证明安全的只读查询，并将其送入统一审批流程。

#### 场景: Plan 模式下受限磁盘容量查询进入审批

- **WHEN** 智能体在 `Plan` 模式下以 `shellKind: "cmd"` 执行 `wmic logicaldisk where "DeviceID='C:'" get Size,FreeSpace /format:value`
- **THEN** 拦截器必须将其识别为受限的只读磁盘容量查询，且在通过结构安全校验后进入统一审批流程。

#### 场景: Plan 模式下含复合符号的命令被直接拒绝
- **WHEN** 智能体在 `Plan` 模式下试图在终端执行 `dir /-C /w C:\Windows\Temp 2>nul | find " 个文件"`
- **THEN** 拦截器判定该命令虽命中只读白名单但因包含复合连接符 `|` 和重定向符 `>`，与执行期结构校验同构判定为不安全，直接返回 `BLOCKED (Plan Mode Only)` 并附带自愈引导提示。

#### 场景: Plan 模式下非白名单命令被拦截并返回自愈提示
- **WHEN** 智能体在 `Plan` 模式下以 `shellKind: "cmd"` 执行 `wmic process list` 或其他非 `wmic logicaldisk` 前缀的命令
- **THEN** 拦截器实施硬性拦截并报错 `BLOCKED (Plan Mode Only)`；且在返回信息中显式拼接提示语，引导智能体使用 `list_dir` 或 `read_file` 等只读 `API` 代替，阻止盲目重试。

#### 场景: Plan 模式下前置判定与执行期校验同构
- **WHEN** 智能体在 `Plan` 模式下请求执行任意终端命令
- **THEN** 命令在前置安全判定阶段必须先完成与执行期结构校验同构的只读安全审查；若通过则该命令才允许进入审批，且审批放行后在 `execute` 阶段必须通过同一批结构约束；若前置判定拒绝则该命令不可能进入后续审批或执行阶段，杜绝"审批通过但执行失败"的假阳性体验。

### 需求: 动态路径越界 Ask 授权与生命周期隔离
当智能体试图访问工作区外的目录或文件时，路径验证器应当（SHALL）挂起当前操作并调用 `Ask` 服务进行提问；若用户确认放行，系统必须将该物理绝对路径在当前会话的 `SessionContext` 下（绑定 busy 锁防护与当前会话 `sessionId`）临时追加到安全服务的 Map 隔离白名单中。此授权仅在当前会话的执行生命周期内放行，并在推理交互完成的 `finally` 终态物理执行该会话临时权限的完全销毁（clear），防止权限残留污染与内存泄漏。

#### 场景: 越界读取 Ask 确认后隔离放行
- **WHEN** 智能体试图读取宿主机上的 `C:\Users\admin\.gitconfig`，路径验证器触发越界
- **THEN** 系统挂起读取并向用户发出询问；当用户允许后，系统将其加入当前 `sessionId` 映射的临时只读白名单并放行；对于非当前会话（如并发的 `other-session`）的访问依然予以拦截拒审；并在当次交互结束退出时物理注销该 `sessionId` 的全部授权
