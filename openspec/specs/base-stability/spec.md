## 新增需求

### 需求: 本地技能文件变更重载稳定性
在 `.agent/skills` 目录下的技能文件遭受多次元数据触碰或高频修改时，系统应当（SHALL）在最后一次事件发生后的 100ms 进行单次热重载，必须（MUST）合并该时间窗内的全部重复事件，以保障系统稳定并杜绝日志洪泛。

#### 场景: 高频事件合并热重载
- **WHEN** 技能文件在数毫秒内连续抛出 10 次变动通知事件
- **THEN** 拦截器和重载器仅在最后一次事件发生的 100ms 后执行一次 `RuleManager` 技能重载，并仅输出一次刷新缓存的日志。
