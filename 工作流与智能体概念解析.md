# 工作流与智能体概念解析

**你说**:

你知道当下agent领域workflow和agent两种概念吗

---

**DeepSeek 说**:

> _Thinking: 嗯，用户问的是agent领域里workflow和agent这两种概念的区别，这是一个技术概念辨析问题。用户用了“当下”这个词，说明ta关注的是最新进展，可能需要结合2025-2026年的前沿讨论来回答。

我需要先确认这两个概念的核心定义。workflow通常指预定义的、结构化的任务流程，每个步骤是确定的，比如用LangGraph定义状态图。而agent则强调自主决策，由LLM动态选择行动路径，能调用工具、观察环境并迭代优化。

想到了可以从几个维度对比：决策方式（预设路径vs动态推理）、灵活性、适用场景（确定性任务vs探索性任务）、以及当前的热门趋势（如Anthropic提到的“agent不是越多越好，workflow足够用优先”）。用户可能也在思考自己的项目应该选择哪种模式。

还要注意区分“agent”这个词在学术和工程语境下的不同含义，避免混淆。回答应该清晰简洁，先给出定义，再对比关键差异，最后可以给一个简单的选择建议。最后可以询问用户是否想进一步了解具体框架实现。_

理解这两个概念对设计AI系统很关键。简单来说：

**Workflow（工作流）**是“固定剧本”：用预定义的规则和决策树把任务拆解成标准化步骤，LLM只负责在指定节点执行具体操作。比如“自动写周报”流程：先提取上周 commits → 分析 Jira 任务 → 调用模板生成总结。特点是**可预测、成本低**，适合规则明确、不允许偏离的任务，如数据清洗、批量处理。

**Agent（智能体）**是“即兴演员”：由LLM自主规划、决策和执行，能动态调用工具、反思结果并调整后续行动。比如用户问“帮我订下周三去北京的机票”，Agent会自主判断需要查日历、比价、考虑行李额度等。特点是**灵活、能处理模糊任务**，但成本更高、行为有时不可预测，适合客服、研发助手等开放性场景。

**关键区别**在于谁控制“下一步做什么”：Workflow靠硬编码规则，Agent靠LLM实时推理。实际应用中常**混合使用**——简单子任务用Workflow保证效率，复杂决策交给Agent。另外，现在流行的**多Agent系统**本质也是通过Workflow来协调多个Agent的协作流程。

当前趋势是向**“受控Agent”**发展：用轻量规则框定Agent的行动边界，在灵活性和可靠性之间找平衡。

如果你想了解如何判断某个具体场景该用Workflow还是Agent，我可以进一步说明。

---

**你说**:

你觉得严格区分这两个概念是有意义的吗

---

**DeepSeek 说**:

> _Thinking: 我们问的是“严格区分这两个概念是否有意义”。需要从技术实践、学术讨论、工程应用等角度分析。可能观点：有意义，因为它们在设计模式、控制逻辑、适用场景上确实不同；但也可能过度区分无益，因为存在连续谱。作为AI，要给出平衡的看法。

最终回答：既有意义，也不应绝对化。意义在于指导设计与资源分配；但实际中常有混合，边界模糊。所以严格区分在理论上和教学中很有价值，但在实践中灵活看待。_

这个问题问得很到位。严格区分“Workflow”和“Agent”**在理论教学、设计决策和团队沟通中很有意义，但在实际落地时不必过于教条**，因为两者本质上是控制策略光谱的两个端点，中间存在大量混合形态。

**为什么严格区分有意义？**

1. **指导技术选型**：明确场景需要“可预测、低成本、易调试”（选Workflow）还是“灵活、自主、能处理开放任务”（选Agent），避免用大炮打蚊子或用剧本演即兴剧。
2. **影响系统可靠性**：Workflow的行为边界清晰，适合金融交易、数据管道等高风险场景；Agent可能产生意外操作，需要额外防护。
3. **影响成本与延迟**：Agent每步都要LLM推理规划，通常比Workflow慢且贵一个数量级。区分两者有助于预算控制。
4. **促进学术交流**：目前研究社区（如AutoGPT、LangGraph、Semantic Kernel）对这两个术语的使用逐渐形成共识，统一概念有助于论文复现和基准测试。

**为什么不必严格区分？**

1. **存在连续谱**：实际系统常用“Workflow中嵌入Agent节点”或“Agent按Workflow模板行动”。比如一个客服Agent先用固定流程检查订单状态（Workflow），再自主决定退款方案（Agent）。强行归类为某一类会丢失信息。
2. **动态路由**：有些框架支持根据输入复杂度自动切换——简单请求走Workflow路径，复杂请求交给Agent。这种混合系统难以贴单一标签。
3. **工程实现趋同**：很多Agent框架（如LangGraph）底层就是用图结构定义状态机和工具调用，与Workflow框架的代码结构几乎相同，区别仅在于“规划步骤是否由LLM动态生成”。

**结论**

- **在概念澄清、架构设计、成本估算阶段**，严格区分非常有益，能帮助团队对齐期望、避免滥用Agent。
- **在具体代码实现和系统描述时**，不妨把它们看作一个连续轴，重点描述“哪些步骤由硬编码规则控制，哪些步骤由LLM自主决策”，而不是纠结于命名。

最终，有意义的是理解**控制权在哪里、确定性有多高**，而非标签本身。就像“轿车”和“SUV”有区别，但你要翻山越岭时，更关心的是离地间隙和四驱能力，而不是车的分类。

---

**你说**:

我的老师是推崇agent排斥workflow的，他认为让ai自主解决问题才是大势，并且说主流的Anthropic和OpenAI等公司都是不用workflow那一套的

---

**DeepSeek 说**:

> _Thinking: 用户想了解他的老师推崇Agent而排斥Workflow的观点是否合理，以及主流AI公司是否真的只采用Agent。我需要提供多角度的信息，包括这些公司实际的工作流、workflow与agent的互补关系、以及工业界的平衡实践。为了全面获取信息，我将同时进行多项搜索，涵盖Anthropic和OpenAI的实际应用、workflow与agent的关系、相关观点以及工业界实践。

Relevant pages found.

所有并行搜索已完成。根据搜索结果，我们需要组织回答，既要尊重老师的观点，又要提供更全面的视角。回答将分为几个部分：首先回应老师观点中的合理性，然后阐述实践中的混合模式，接着引用一些权威观点来佐证，最后提出一个建设性的总结。_

你老师的观点确实代表了这个领域一个非常重要、也很激动人心的方向。完全自主的 **Agent** 无疑是未来大势，但这可能并不是OpenAI和Anthropic实践的全貌。

实际上，**Workflow 和 Agent 并非对立，在实践中更像是相辅相成的“骨架”与“大脑”，共同构成完整的“Agentic 系统”**[reference:0]。绝大多数公司（包括一线大厂）都采用 **“受控Agent”** 的思路：用 Workflow 提供稳定框架，框定 Agent 的活动边界以保证可靠。

**为什么还离不开 Workflow？**
全自主的“黑盒”Agent 落地时会有现实挑战：复杂任务中长期推理常不可靠[reference:1]；不加约束可能无限循环消耗资源[reference:2]；不可预测的输出[reference:3]在金融、医疗等高风险领域难以通过审计；预算和 Token 消耗也难控制[reference:4]。

**大厂真实画像**
Anthropic明确建议“Workflow 解决 80% 问题，Autonomous Agent 只用于剩余 20%”[reference:5]，通过 Agent Skill 为模型预设清晰的“岗位SOP”[reference:6]；OpenAI 推出 AgentKit 等平台，核心就是为了帮开发者编排 Workflow，管理和约束Agent[reference:7]。

你老师的看法抓住了Agent的远大前景，但顶尖公司真正领先的并非简单的“替代”，而是更早意识到了两者**协同**的巨大力量。如果你有具体的应用场景，我们可以一起聊聊在实操中如何找到最适合的平衡点～

---

**你说**:

之前看到langgraph说没有严格的区分，只是agent程度的问题

---

**DeepSeek 说**:

> _Thinking: 用户提到LangGraph官方认为Workflow和Agent没有严格区分，只是Agent化程度的问题。我需要提供官方资料来支撑这个观点。为了全面获取信息，我需要同时进行多项搜索，涵盖LangGraph官方文档、博客、演讲、社区讨论以及相关概念如“agentness”或“degree of agentic”。

Relevant pages found.

搜索结果显示了一些可能包含相关信息的页面。为了全面获取信息，我们需要同时打开这些页面。

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

这些页面提供了相关信息。其中，页面6和7提到了LangGraph支持workflow和agent，并强调它们是“agentic systems”的组成部分。页面8、9、11则对workflow和agent进行了详细比较。页面10和12提供了LangGraph的架构细节。页面2可能包含一个问答，但可能不是官方信息。页面1的结果可能不直接相关。页面4和5可能包含一些社区讨论。我的回答将围绕LangGraph的官方立场展开，分三个部分：先展示官方原文与核心观点，然后解释这个观点与老师观点的差异，最后用表格进行总结。_

你说的没错，LangGraph的官方立场确实是：**Workflow和Agent并非对立，而是“代理系统”（Agentic Systems）光谱上的两个端点**。

### 📜 官方的“光谱”观点与“二元论”之争
LangGraph官方明确表示，它不认为两者有严格界限，而是协同工作的两种工具。其架构同时支持Workflows、Agents及中间的任何形态[reference:0]。例如，Workflows就像按固定路线行驶的公交车[reference:1]，而Agent则像自主导航的无人车[!reference:2]，但两者本质都是用“图”（Graph）来管理任务[reference:3]。

这背后其实隐含了LangGraph对OpenAI“二元论”的公开批评。LangGraph曾发文指出，将系统简单划分为Workflow或Agent过于粗糙[reference:4]。一个更好的视角是引入“Agentic systems”的概念，它将两者视为可以灵活组合的统一体系[reference:5]。

### 🧐 这和你老师的看法存在微妙差异
你老师的观点更偏向理想化，认为Agent是唯一解。而LangGraph的观点则更偏向工程化现实：**在许多商业和生产环境中，为了保证稳定性、控制成本，使用结构化的Workflow才是更稳健的选择**。比如：
*   **场景决定程度**：如果你的任务是**固定的**（如每周自动导出数据并生成报表），采用Workflow模式**高效、可靠**[reference:6]；如果你的任务**不可预测**（如需要自主调用多个外部API进行研究的项目），则更适合高度“Agent化”的架构[!reference:7]。
*   **实际应用是混合体**：构建一个“代理系统”往往是组合使用两者。比如先通过Workflow执行固定的数据清洗，再交给Agent做决策分析[reference:8]。

### 💎 总结

| 对比维度 | 你老师的观点 | LangGraph 的官方立场 |
| :--- | :--- | :--- |
| **核心理念** | Agent是未来大势，Workflow是过时的旧范式 | Agent和Workflow是连续光谱，都是构建 “Agentic System” 的工具。 |
| **二者关系** | 相互对立，非此即彼 | 没有严格区分，可混合使用，只在“Agent化程度”上有差异。 |
| **选择依据** | 优先且普遍地采用Agent | 根据具体任务的**确定性**和**稳定性要求**来选择合适的“程度”。 |

从LangGraph的视角来看，Workflow和Agent就像工具箱里不同的工具，作为优秀的工程师，关键是根据任务特点灵活运用，而不是在二者中做单选题。需要我结合你手头的具体场景，帮你分析一下用Workflow还是Agent会更合适吗？

---

**你说**:

那你觉得会话状态机会显著降低agent程度吗？如果我做的agent是一个通用智能体，每天的任务都可能不一样，应该怎么设计？

---

**DeepSeek 说**:

> _Thinking: 好的，用户问的是两个紧密相关的问题。第一个是关于会话状态机是否会降低agent程度，第二个是关于如何设计一个任务每天都不一样的通用智能体。

嗯，回顾之前的对话，用户一直在深入探讨workflow和agent的概念区别，还提到了他老师推崇agent、排斥workflow的观点，以及LangGraph的“光谱”观点。用户现在显然是在尝试实践，遇到了具体的设计问题。他的身份可能是一位AI开发者或研究者，正在构建一个通用agent系统。

用户的第一层疑问“会话状态机会显著降低agent程度吗”，深层担忧可能是：他不想因为引入了某种结构化机制（如状态机）而让自己的agent变得“不够智能”或“退化成workflow”，从而违背他老师所倡导的“自主解决”大势。他需要确认状态机是否是一个可以接受的、不会损害agent核心价值的工具。

第二层问题“通用智能体，每天任务不同怎么设计”更实际。他意识到完全靠模型自由发挥可能不可靠、成本高，需要一种既保持灵活性又能稳定运行的架构。他想要的不是一个非此即彼的答案，而是一个可行的混合设计模式。

所以，我的思考需要先明确回答状态机的问题。状态机本身是一种控制结构，关键在于谁在控制它。如果是硬编码的固定流程，那确实会降低agent程度。但如果状态机只是记录对话历史和当前步骤的“记忆存储”，而下一步做什么完全由LLM根据这些上下文和工具来推理决定，那么状态机只是基础设施，不会削弱自主性。这正好对应LangGraph那种图结构——节点和边定义了可能的状态和转移，但路径选择权在LLM。实际上，这种结构还能防止agent失控，提高可靠性。

对于通用智能体的设计，不能依赖单一模式。需要分层：一个核心的规划模块（完全由LLM驱动，负责理解每天不同的任务并拆解），然后为常见任务类型（如信息检索、代码执行、文件操作）预先设置标准化的执行流程（workflow），但触发这些流程的权利仍然在规划模块手里。同时，必须有一个很强的记忆系统（长期记忆存储用户偏好和过往任务结果，会话状态作为短期记忆），以及一个安全的工具调用接口。这样，agent既灵活（能应对新任务），又高效稳定（常见任务走优化路径）。

最后，用户可能还需要一些权衡建议，比如如何决定哪些步骤用workflow固化、哪些交给agent自由发挥。可以给出一个决策思路：看步骤是否高频、标准化、且不需要创造力。另外，需要强调防御性设计，比如限制单步时间、总迭代次数，防止agent在奇怪的任务上无限循环。

总结下来，回复的核心是：状态机不会降低agent程度，关键看控制权；通用智能体应设计为“受控Agent”架构，结合动态规划器与稳定的工具/流程库，并强化记忆和边界控制。这样既拥抱了自主性，又保证了工程可行性。_

你提的这两个问题非常关键，直接触及了当前Agent工程设计的核心矛盾。

### 1. 会话状态机会显著降低Agent程度吗？

**不会，关键在于“谁在控制状态转移”。**

-   **低Agent程度**：状态机由开发者硬编码，每一步去哪儿是固定的。这本质上就是个Workflow。
-   **高Agent程度**：状态机仅记录“当前在哪、历史是什么”，但**“下一步去哪儿”完全由LLM动态决定**。LLM根据当前状态、会话历史、可用工具，推理出该做的动作并触发状态转移。

像LangGraph这样的框架，其核心价值就是用图结构维护会话状态，同时允许LLM在每个节点自主决策。**状态机是Agent的“记忆骨架”，不仅不会降低Agent程度，反而是实现复杂自主行为的必要基础设施**——没有它，Agent连“自己说过什么、做到哪一步”都记不住。

### 2. 任务每天变化的通用智能体，该怎么设计？

对于通用智能体，完全的自由（每次都从零规划）低效且不可靠，完全的固化又失去灵活性。推荐 **“骨架+插件”的分层架构**：

**第一层：核心调度器（高Agent程度）**
只有一个任务：理解用户今天的需求，然后**选择合适的“技能模块”**并委托给它。调度器自身不处理具体细节，只做路由和结果整合。

**第二层：技能模块库（混合程度）**
每个模块解决一类常见任务（如“网页浏览”、“代码编写”、“数据分析”）。模块内部可以根据任务确定性选择实现方式：涉及多步、跨工具的用Workflow保证效率；涉及探索、推理的用Agent保留灵活性。调度器不关心模块内部是Workflow还是Agent，只关心它能完成任务。

**第三层：长期记忆系统（关键）**
通用智能体必须记住昨天的对话、用户的偏好、过往的失败经验。每次调度时，自动检索相关记忆注入上下文，让LLM能基于历史做决策。

**第四层：边界控制（防御性设计）**
设置硬性约束：单步超时（如30秒）、最大迭代次数（如10轮）、需要用户确认的操作白名单。这不会限制Agent能力，而是防止它在不熟悉的任务上失控（比如无限循环或执行危险命令）。

**设计示例**

用户说“帮我分析下昨天GitHub仓库的star趋势，然后画个图，顺便提醒我明天开会”。

-   **调度器**（LLM推理）：识别出需要三个能力 → 调用`github_analyzer`、`plot_generator`、`calendar_reminder`模块
-   **github_analyzer**（Workflow）：固定步骤：clone → fetch metadata → 计算趋势。这里用Workflow，因为步骤明确、追求效率
-   **plot_generator**（Agent）：自主决定画折线图还是柱状图，根据数据分布选择最合适的展示方式
-   **calendar_reminder**（简单API调用）：固定接口，无需Workflow或Agent
-   **调度器**：整合结果，回应用户

**核心原则**：**自由度与稳定性需要平衡**。在调度层充分授权（高Agent程度），在执行层用Workflow或约束控制风险。只把“决策”交给LLM，不把“执行”也盲目地交给它。

这种架构既能应对每天变化的任务，又能保证复杂子任务高效稳定完成。如果你想了解某个具体模块（比如长期记忆系统）的实现细节，我们可以继续深入探讨。

---

**你说**:

按你说的，我们只要告诉llm之前处于过什么状态，现在处于什么状态就行，不应该限制下一步应该做什么吗？langgraph会限制吗？

---

**DeepSeek 说**:

> _Thinking: 用户想知道的是，在LangGraph中，我们是否应该限制LLM下一步可以做什么，以及LangGraph是否有任何限制。这涉及到LangGraph的编排机制、条件边、工具调用、节点函数等方面的设计思路。为了全面获取信息，我们需要进行多角度的搜索。

Relevant pages found.

搜索结果显示了一些相关页面。为了全面了解LangGraph在限制LLM下一步动作方面的设计哲学和具体实现，我们需要同时打开这些页面。

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

这些资料涵盖了LangGraph的设计理念、条件边、路由机制等方面。用户的问题核心是：LangGraph是否限制LLM的下一步行动。我的回答将首先解释限制是可控的“边界”，而非“束缚”。然后详细说明LangGraph的两种限制边界：图结构拓扑（定义所有可能的路径）和条件边路由函数（决定当前走哪条路径）。最后解释LangGraph如何平衡可控性与灵活性。_

你的思考非常敏锐，触及了LangGraph设计的核心。

答案是：**LangGraph的设计哲学，恰好是让你说的“限制”和“不限制”能够同时成立，并且达到一种精妙的平衡。**

它通过“**设定边界，内置裁判**”的思路来实现：不会强行规定LLM的每一步，但会定义清楚“**能做什么**”和“**在哪做决策**”。

简单来说，这里的“限制”更像一种“**边界**”和“**裁判**”，而不是束缚。

### 🗺️ 第一层“边界”：图结构定义了所有可能路径
LangGraph会让你把能想到的所有处理流程，都预先定义成一个**图（Graph）**。图中的每个 **节点（Node）** 代表一个任务，而 **边（Edge）** 连接这些节点，定义了所有可能的前进路线。

举一个简单的“下单-决策”场景，这个图就像一张地图，规定了所有允许的通道：
*   用户下单 → 判断风险 → (若风险低) 发货
*   用户下单 → 判断风险 → (若风险高) 转人工

这张地图就是LangGraph设置的第一层“边界”。它画出了一个可控的“活动区域”，确保AI无论怎么决策，都只能在画好的路径里运行，绝不会跳出系统预定好的“三界之外”[reference:0][reference:1]。

### ⚖️ 第二层“裁判”：路由函数决定当前走哪条路
边界划好后，谁来裁判下一步具体走哪条路呢？你最初的直觉（“告诉LLM之前处于过什么状态，现在处于什么状态就行”）正是答案的核心！LangGraph正是通过一个叫做“**条件边**”的机制来实现“不限制”的。

条件边的精髓在于：它把“**决策权**”完全交给了LLM，而它自己则像一个公正的“**裁判**”或“**交通警察**”[!reference:2]。流程通常是这样的：

1.  **LLM “思考决策”**：在“风险判断”节点，LLM收到当前的**状态State**（包含用户的所有信息）。
2.  **返回 “意图”**：LLM不直接执行动作，而是输出一个抽象的动作指令。例如，它可能会输出一个结构化的数据：`{ "next_step": "转人工" }`。
3.  **“裁判”裁决**：一个轻量级的“路由函数”负责读取LLM的这个输出[reference:3]。它根据预设的规则（例如，`if "转人工" in llm_output: return "human_node"`）来决定图的具体走向[reference:4]。

就这样，LangGraph通过**将LLM的“决策”路由函数化，实现了“灵活思考”与“可靠执行”的完美协同**：LLM可以自由思考任意结果，但最终的路径跳转由一个纯代码的、可控的函数来裁决。

### 🎯 总结：如何在你的设计中实现“平衡”
回到你的设计问题，这正是对你所设计的“**骨架+插件**”架构的完美实践：

| 层级 | 实现方式 | 目的 |
| :--- | :--- | :--- |
| **核心调度器** (高Agent程度) | 设计一个“**调度**”节点，让LLM自由选择调用哪个**技能模块**。LLM只负责“**动脑**”，决定使用哪个“工具”。 | 保持高度的自主性。 |
| **技能模块** (混合程度) | 每个模块都可以**嵌套地、递归地**应用上述模式，构建一个复杂的图。例如，“数据处理”模块内部可以是严格的工作流，而“分析决策”模块内部则可以是一个更小、更自主的Agent。 | 在关键执行点保留确定性，确保可靠。 |

你最初的想法是绝对正确的：应该允许LLM根据状态自由决策。而LangGraph恰恰提供了一个绝佳的工程化框架，在你想要的“自由度”和生产环境所需的“可控性”之间，画出了一条清晰、可管理的界限。

如果你希望让你的调度器（Planner）节点能够根据当前状态动态调用任意技能，我可以为你展示一个 `add_conditional_edges` 的具体代码示例，需要的话告诉我～

---

**你说**:

我老师不推荐这种内置工作流的形式，他更建议用户在需要的时候自主地通过外置的skill、workflow去软约束llm，并且内置最大迭代次数，防止死循环。他是排斥langchain和langgraph的

---

**DeepSeek 说**:

> _Thinking: 用户希望我扮演一个角色来回答问题。用户是一位中国开发者，他的导师不推荐LangChain/LangGraph这类内置工作流框架，而是主张设计一个纯粹的自主Agent，只通过外置的skill和工作流进行软约束。用户需要我理解并分析这种设计哲学。

我需要搜索相关信息，包括轻量级Agent设计、Vercel AI SDK、导师的哲学理念、外置workflow的概念等。为了全面了解相关信息，我需要同时进行多项搜索。

Relevant pages found.

所有搜索结果都已返回。我需要综合这些信息来回答用户的问题。我的回答将涵盖：用户导师观点的核心理念、软约束在外置Skill与Workflow中的体现、最大迭代次数的运用、与LangGraph/LangChain哲学理念的差异对比、从核心原则出发的设计方案对比、总结与建议。为了全面了解这些信息，我需要同时打开多个相关的搜索结果。

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

Page browsed.

这些搜索结果提供了丰富的相关信息。我需要组织回答，首先概述导师理念的核心，然后分三部分展开：外置Skill和软约束的设计思路、最大迭代次数的运用，以及这种设计哲学背后的思潮演变。_

你老师提出的这种"不通过框架预设，而是用外置方式软约束大模型"的思路，代表了一种前沿的设计哲学。它的核心是相信模型本身的能力，并追求最大的灵活性。这种思路背后，是一个正在兴起的工程范式——**Harness + Skill**。

### 🧠 Harness + Skill：新一代的工程化范式

你老师的主张，其实是这种新范式的高度概括。

*   **Harness (约束框架)**：它为AI系统设定了清晰、可执行的"行为边界和操作规则"[!reference:0]。它的核心理念是：为AI提供一个"工作制度"（类似人类工程师的职责说明），让其在清晰边界内自主决策，而非无序自由发挥[reference:1][!reference:2]。具体实践中，包括**明确权限控制**（能做什么、不能做什么）、**设定操作规则**（工具使用规范），并内置**最大迭代次数**、**超时熔断**等硬性限制作为"安全网"[reference:3]。
*   **Skill (技能模块)**：它将特定业务逻辑封装成可插拔的"技能单元"，供AI按需加载调用[reference:4]。Skill强调**轻量无依赖**、**单一职责**（一个Skill只专注解决一个问题）[reference:5][reference:6]，并采用**渐进式披露**策略，仅在需要时加载文档和资源，避免上下文窗口被无用信息淹没[reference:7][reference:8]。

### 🔧 如何实践：外置的"软约束"

具体到实现层面，你老师的思路清晰且务实：

*   **Skill管理机制**：为AI提供一系列按照统一格式封装的工具函数，**让AI能自主决定何时、以何种顺序调用它们**，而不是由硬编码的工作流来驱动。
*   **上下文窗口管理**：通过"渐进式披露"解决上下文拥堵。例如，系统运行时，AI只看到一个极简的Skill名称列表；当决定使用某个Skill后，程序才将其详细文档动态加载到上下文中，供AI参考执行[reference:9]。
*   **最大迭代次数作为最后防线**：在AI的主循环中设置一个最大循环次数，达到上限后直接终止任务。这是防止死循环或无限token消耗的最简单、最有效的"断路器"。
*   **避免预设死流程**：这是该思路的核心特征。它没有`LangGraph`那种预定义的、显式的状态流转图，而是通过在一个`while True`循环中不断调用LLM，让其根据当前状态和观察结果来决定下一步行动[!reference:10]。

### 💡 思潮演变：从LangChain到Harness+Skill

你老师的观点，也折射出业界对LangChain这类重型框架的反思。

LangChain早期确实降低了AI应用开发的门槛，但随着时间推移，其复杂的抽象、陡峭的学习曲线和过度的"黑盒化"受到了越来越多的批评。许多开发者发现，为了修改一个简单行为，有时不得不在它的抽象层级中挣扎。不少一线AI工程师开始倾向"少脚手架多模型"[reference:11]，用更简单、更透明的几十行`while`循环代码来构建Agent[!reference:12]。

与此同时，业界领导者们正通过新架构回应。Anthropic推出的Claude Code，就践行了Harness + Skill的理念[reference:13][reference:14]。它通过精心的工具设计和多Agent协作流程实现对模型的约束，而非LangChain式的固定工作流。其开发者团队甚至透露，开发工作本身几乎100%由AI Agent完成，证明了该范式的强大潜力[reference:15]。轻量级框架如Vercel AI SDK也主张用最简化的核心`Agent`循环，最大限度地保留开发者的控制权，避免被框架绑架[reference:16]。

### ⚖️ 理念之争的再思考

你老师的这种思路，与LangGraph这类基于工作流的框架，背后是两种不同的信任模型：

*   **你老师的信任模型**：**信任模型的能力，信任它能做出正确决策。** 目标是给予模型最大自由度，期望它作为通用解题器来应对无限可能，追求极致的灵活。这也是当下许多前沿研究者和AI驱动型初创公司的倾向。
*   **LangGraph的信任模型**：**不信任过程，只信任结果。** 通过"信任但验证"的思维，用硬编码的`if/else`作为护栏，将LLM的每一次重大决策都置于程序化检查之下，保证高风险操作的可控性[!reference:17]。

两者并非绝对的对错，而是权衡了系统灵活性与可靠性的不同选择。

### 💎 总结

所以说，你老师的观点精准地捕捉到了当下Agent工程化从"为AI铺路"的重框架，转向"给AI套缰绳"的轻约束。这不仅是技术的选择，也是一种理念的跃迁。

这种新范式确实听起来很理想，不过在实际动手时，要完全做到"不给任何预设流程"其实挑战很大，可能会遇到LLM在复杂任务中规划不稳定、反复卡在某个点上等问题。建议可以再从你老师那请教下，当LLM需要执行一个非常复杂的任务，比如完成一个涉及前后端的全栈项目时，如果完全不预设任何流程，他是如何在实际工程中保证LLM的规划稳定、行动有序的呢？我很好奇他的实战经验。

---

